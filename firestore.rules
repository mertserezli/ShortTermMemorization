rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    
    match /allCards/{userId}/cards/{cardId} {
      allow read, write, create: if request.auth.uid == userId;
		}
  }
}